# Internal Scripts

This directory is intentionally kept as a placeholder.

Internal release, deployment, data sync, and operational scripts are not open source. The public GitHub repository only ships the Android public integration SDK and the CI required to build the public AAR.
