apiVersion: 1

datasources:
  - name: Prometheus
    uid: leona-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      httpMethod: POST
