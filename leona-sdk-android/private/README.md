# Android Private Runtime

This directory is intentionally kept as a placeholder.

The private Android runtime, private native detector catalog, private JNI bridge, and high-value heuristics are not open source. They are excluded for security reasons so the public SDK can be integrated without publishing bypass-relevant implementation details.

The public Android SDK remains usable without this directory. Private builds can provide an internal replacement module at this path.
